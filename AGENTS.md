# Agent instructions

## Git workflow

- Do not commit directly to `master`.
- Put every change on a new branch (one branch per task or related set of changes).
- When the work is complete, open a pull request targeting `master`.
- Do not merge the PR unless the user asks you to.
